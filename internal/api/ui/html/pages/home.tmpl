{{define "title"}}Search Order{{end}}
{{define "main"}}
<div class="container">
    <div class="card">
        <h1>Search for an Order</h1>
        <form id="search-form">
            <input type="text" id="order-uid" name="order_uid" placeholder="Enter Order UID" required>
            <button type="submit">Search</button>
        </form>
    </div>
</div>
{{end}}
